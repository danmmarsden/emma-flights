{
  "name": "arrivals-departures",
  "version": "1.0.0",
  "private": true,
  "description": "Simple Leeds Bradford Airport arrivals and departures viewer",
  "scripts": {
    "generate:data": "node scripts/generate-data.js",
    "start": "python3 -m http.server 3000 --directory public"
  }
}
